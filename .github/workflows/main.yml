on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:

    - name: "Checkout"
      uses: actions/checkout@v6
      with:
        fetch-depth: 0
    - name: "Deploy"
      uses: milanm/actions-file-deployer@master
      with:
        remote-protocol: "sftp"
        remote-host: "dev.fast.sheridanc.on.ca"
        remote-user: "swallowk"
        ssh-private-key: ${{ secrets.DEPLOY_PRIVATE_KEY }}
        remote-path: "/data/home/swallowk/public_html"